import PropTypes from 'prop-types';

export const ReactRoutePropTypes = {
  /** react-router-config */
  location: PropTypes.object,
  /** react-router-config */
  history: PropTypes.object,
  /** react-router-config */
  match: PropTypes.object,
  /** react-router-config */
  route: PropTypes.object,
};
