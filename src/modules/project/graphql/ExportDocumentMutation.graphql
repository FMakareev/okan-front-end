mutation ($id: String!) {
  exportdocument(id: $id) {
    document {
      createrevisiondate
      documentnumber
      equipmentname
      id
      name
      okancode
      project
      internalMatching {
        id
        firstname
        lastname
        email
      }
      version
    }
  }
}
