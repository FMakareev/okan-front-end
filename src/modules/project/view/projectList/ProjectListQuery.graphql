query ($pageSize: Int, $pageNumber: Int) {
  projectList(pageSize: $pageSize, pageNumber: $pageNumber) {
    id
    name
    author {
      id
      firstname
      lastname
    }
    createdate
    updatedate
    isTemplate
  }
}
