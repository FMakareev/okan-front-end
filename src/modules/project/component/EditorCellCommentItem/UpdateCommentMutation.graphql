mutation ($id: String!, $isdelete: Boolean) {
  updatecomment(id: $id, isdelete: $isdelete) {
    comment {
      createdate
      id
      isdelete
      message
      sender {
        firstname
        lastname
        patronymic
        id
      }
      updatedate
    }
  }
}
