mutation ($id: String!, $name: String!, $partners: [String]) {
  updateproject(id: $id, name: $name, partners: $partners, ) {
    project {
      id
      name
      partners {
        id
        firstname
        lastname
      }
    }
  }
}
