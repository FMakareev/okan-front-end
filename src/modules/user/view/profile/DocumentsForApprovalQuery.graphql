query ($user: String,$pageSize: Int, $pageNumber: Int) {
  documentsForApproval: documentsforapproval(user: $user,pageSize: $pageSize, pageNumber: $pageNumber) {
    id
    name
    okancode
    project
    approvalstatus
  }
}
