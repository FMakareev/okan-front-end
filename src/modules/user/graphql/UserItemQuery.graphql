query ($id: String) {
  useritem(id: $id) {
    birthdate
    email
    firstname
    id
    lastname
    patronymic
    phone
    position
    role {
      id
      name
      resolvers {
        name
        id
      }
    }
    signature
  }
}
