mutation($password: String!, $newPassword: String, $confirmNewPassword: String) {
  recoverypass( password: $password, newPassword: $newPassword, confirmNewPassword: $confirmNewPassword){
    recoverypass{
      id
      email
    }
  }
}
