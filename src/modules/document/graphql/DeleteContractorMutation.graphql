mutation ($id: String!) {
  deletecontractorapproval(id: $id) {
    contractorapproval {
      id
      approvaldate
      user {
        id
        firstname
        lastname
        patronymic
        position
        organizationname
        signature
        role{
          id
          name
        }
      }
    }
  }
}
