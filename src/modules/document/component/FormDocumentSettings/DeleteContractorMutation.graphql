mutation ($id: String!) {
  deleteContractorApproval: deletecontractorapproval(id: $id) {
    contractorapproval {
      id
      approvaldate
      user {
        firstname
        lastname
      }
    }
  }
}
